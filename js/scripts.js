// scripts.js

function drawTree(a) {
    for (var i = 0; i < a; i++) {
            var star = '';
            for (var j = 0; j <= i; j++) {
                var star = star + '*';
            }
        console.log(star);
        }
    }
